<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC
        "-//Checkstyle//DTD SuppressionFilter Configuration 1.2//EN"
        "https://checkstyle.org/dtds/suppressions_1_2.dtd">

<suppressions>
    <!--
      Optional per-file or per-check suppressions. Prefer narrow entries; remove when the issue is fixed.

      Previously: package-wide LineLength for pojo/ — removed; add a single-file suppress only if needed.
    -->
</suppressions>

