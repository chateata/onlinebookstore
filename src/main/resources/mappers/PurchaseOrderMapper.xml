<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.bookshop.dao.PurchaseOrderMapper">
  <resultMap id="BaseResultMap" type="com.shop.bookshop.pojo.PurchaseOrder">
    <id column="po_id" property="poId" />
    <result column="supplier_id" property="supplierId" />
    <result column="order_date" property="orderDate" />
    <result column="expected_arrival_date" property="expectedArrivalDate" />
    <result column="status" property="status" />
    <result column="total_amount" property="totalAmount" />
  </resultMap>
  <sql id="Base_Column_List">po_id, supplier_id, order_date, expected_arrival_date, status, total_amount</sql>
  <select id="selectByPoId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" /> from purchase_order where po_id = #{poId}
  </select>
  <insert id="insert" parameterType="com.shop.bookshop.pojo.PurchaseOrder" useGeneratedKeys="true" keyProperty="poId">
    insert into purchase_order (supplier_id, order_date, expected_arrival_date, status, total_amount)
    values (#{supplierId}, #{orderDate}, #{expectedArrivalDate}, #{status}, #{totalAmount})
  </insert>
  <update id="updateByPoId" parameterType="com.shop.bookshop.pojo.PurchaseOrder">
    update purchase_order
    <set>
      <if test="status != null">status = #{status},</if>
      <if test="totalAmount != null">total_amount = #{totalAmount},</if>
    </set>
    where po_id = #{poId}
  </update>
</mapper>


